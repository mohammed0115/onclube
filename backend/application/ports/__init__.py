"""Ports — abstract interfaces the application layer depends on.

Repositories abstract persistence; gateways abstract external systems
(notifications, video, AI, file storage, events). Infrastructure provides the
concrete adapters (dependency inversion).
"""
