# English Club — backend (Phase 5 skeleton)
Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-environ==0.11.2
psycopg2-binary==2.9.9          # PostgreSQL driver (production)
gunicorn==22.0.0                # WSGI server (production)
whitenoise==6.7.0               # Static file serving from the app process
redis==5.0.8                    # Optional cache/broker (used when REDIS_URL is set)
openai==1.109.1                 # OpenAI SDK — placement assessment + session reports (heuristic fallback if unset)
sentry-sdk==2.14.0              # Error monitoring — no-op unless SENTRY_DSN is set
agora_token_builder==1.0.0     # Agora RTC token minting (optional; stub fallback if unset)

# Dev / test
pytest==8.2.0
pytest-django==4.8.0
